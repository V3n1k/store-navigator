# Example environment variables for admin frontend
# Set REACT_APP_API_BASE_URL to your backend API base URL (no trailing slash)
# e.g. in development:
# REACT_APP_API_BASE_URL=http://localhost:8080
REACT_APP_API_BASE_URL=http://localhost:8080

# Optional: if you persist tokens in localStorage using key 'token', this client will use it.
# In production, point REACT_APP_API_BASE_URL to your HTTPS API host.
